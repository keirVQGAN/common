streamlit==1.33.0               # Streamlit for running the app
prettymaps>=0.1.3               # Prettymaps for generating maps (includes osmnx, pandas, pyogrio)
python-slugify==8.0.1           # For slugifying location names to safe file names
